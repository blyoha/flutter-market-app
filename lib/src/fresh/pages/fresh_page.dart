import 'package:flutter/material.dart';

class FreshPage extends StatelessWidget {
  const FreshPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("FRESH SCREEN"));
  }
}
